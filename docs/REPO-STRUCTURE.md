# Repo Structure

- `core/` — orchestration, continuity, trust, routing
- `apps/` — app contracts, adapters, and external tool surfaces
- `schemas/` — canonical event, state, and settings schemas
- `config/` — environment and policy defaults
- `docs/` — architecture and product definition
